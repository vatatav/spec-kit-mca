Example Specs (Read-Only)

This folder contains tiny, read-only example specifications to help new users
understand the Spec-Kit workflow without running `/specify` immediately.

Guidance
- Keep examples very small (< 50 KB total) to avoid bloating packages.
- Do not include project secrets or environment-specific paths.
- Mark examples as read-only in documentation; they are not meant to be edited.

